# Exclude CI / non-library artifacts
exclude .github/*
exclude .github/*/*

# Exclude heavy / dev-only artifacts
exclude benchmarks/*
exclude benchmarks/*/*
exclude heatmaps/*
exclude *.csv
exclude *.png
exclude *.jpg
exclude *.jpeg
exclude *.gif
